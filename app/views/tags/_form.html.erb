<%= form_for(tag, :html => {:role => "form"}) do |form| %>

<%= render './layouts/form_error', :object => tag %>

<div class="form-group">
  <%= form.label :name, '名称' %><br>
  <%= form.text_field :name, :class => "form-control" %>
</div>

<div class="form-group">
  <%= form.label :order, '编号' %><br>
  <%= form.number_field :order, :class => "form-control" %>
</div>

<div class="well">
  <div class="btn-toolbar">
    <%= form.submit('保存', :class => "btn btn-default") %>
  </div>
</div>

<% end %>