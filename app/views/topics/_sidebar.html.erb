<ul class="nav nav-pills nav-stacked">
  <% Tag.all.each do |tag| %>
  <%= sidebar_link_to tag.name, tag_path(tag), tag.topics.count %>
  <% end %>
</ul>

<ul class="nav nav-pills nav-stacked">
  <%= sidebar_link_to '标签管理', tags_path %>
  <%= sidebar_link_to '全部的帖子', topics_path %>
  <%= sidebar_link_to '无标签的帖子', untaged_topics_path %>
  <%= sidebar_link_to '查找', search_topics_path %>
</ul>