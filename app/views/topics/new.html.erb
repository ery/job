<%= draw_action_header %>

<%= form_for(@topic, :html => {:role => "form"}) do |f| %>

<%= render 'form', :topic => @topic, :form => f %>

<div class="well">
  <div class="btn-toolbar">
    <%= f.submit('Create', :class => "btn btn-default") %>
    <button type="button" class="btn btn-default"><%= link_to('Back', topics_path) %></button>
  </div>
</div>

<% end %>
