
<% if object.errors.any? %>
<div id="error_explanation">
  <h2>发现<%= pluralize(object.errors.count, "个错误") %>, 请在修改好再提交:</h2>
  <ul>
    <% object.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
